---
import ServicePage from '../../components/pages/ServicePage.astro';
import { servicePaths } from '../../lib/paths';

export const getStaticPaths = servicePaths('en');

const { service, locale } = Astro.props;
---
<ServicePage service={service} locale={locale} />
